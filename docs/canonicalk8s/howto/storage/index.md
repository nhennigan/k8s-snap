# Storage

Most Kubernetes clusters will need some type of persistent storage for running
workloads. These guides contain information on setting up storage, or using the
default storage built-in to {{product}}.

**In terms of navigation, make the common ones the same page - default storage and ceph, Then have variations**

So side nav will be 
Use default storage 
Use Ceph storage 
Use cloud storage 

On this page you can see then what applies to what type

## Default storage 

```{toctree}
:titlesonly:

Use default storage <storage.md>

```


## Snap based storage 

```{toctree}
:titlesonly:

Use Ceph storage <ceph.md>
Use cloud storage <cloud.md>
```

## Charm integration storage

```{toctree}
:titlesonly:

Use Ceph charm <ceph-csi.md>
```